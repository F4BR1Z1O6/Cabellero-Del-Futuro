using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Audio : MonoBehaviour
{
    // Este método se llama una vez al inicio del juego, justo antes de que se ejecute el primer frame.
    // Actualmente no realiza ninguna acción.
    void Start()
    {
        // Inicialización del componente o configuración de variables iría aquí.
    }

    // Este método se llama una vez por cada frame. 
    // Es ideal para manejar lógica que debe actualizarse constantemente, como controles de usuario o movimiento.
    void Update()
    {
        // Aquí se podría actualizar el estado del audio o detectar si debe reproducirse algún sonido.
    }
}

